# include <stdio.h>

/* Simple Hello World program */
int main(){
    /* Using puts() instead of printf() for simple string output
     * puts() automatically adds a newline and is more efficient
     * for outputting string literals without formatting */
    puts("Hello, World!");
 
    return 0;
}
